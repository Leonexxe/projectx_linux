playanimation("startup")
connect("projectXDataBase.com")::onfail(shutdown("1", "cannot reach database"))
connect("projectX.com")::onfail(initOfflineMode("noConnection"))
connect("projectXStatus.com")::onfail(notify("cannot reach status server(projectXStatus.com)"))
connect("projectXNotify.com")::onfail(notify("cannot reach notification server(projectXNotify.com)"))
// append "connect([IP/domain])" to initate additional connections on startup

// add "::onfail(shutdown([error code(int)], [reason]))" to shut the application down in case the task fails
// add "::onfail(notify([message]))" to send a notification to the user in case the task fails
// add "::onfail(initOfflineMode([reason]))" to start the application in offline mode in case the task fails